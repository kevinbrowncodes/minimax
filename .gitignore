node_modules/
.DS_Store
.env
.env.*
!.env.example

# Recon: the owner's browser session and raw captures never enter git (CLAUDE.md §3e, §4b)
recon/.profile/
recon/out/

# Model weights live on the Spark's disk, never here (CLAUDE.md §4a)
models/
*.safetensors
*.gguf
*.ckpt
*.pt
*.bin

# Generated videos are never committed; the stub's tiny fixture is the one exception
*.mp4
*.webm
*.mov
!tools/**/fixtures/*.mp4
!tools/**/fixtures/*.webm

# Build and test output
coverage/
coverage-integration/
dist/
.turbo/
playwright-report/
test-results/

# The Spark: weights, outputs, logs and smoke clips for the ComfyUI container live here, never in git (STORY_005)
spark/data/

# Next.js build output and generated types (STORY_007)
.next/
out/
next-env.d.ts
*.tsbuildinfo

# The one-off real trial's report and results (app/playwright.trial.config.ts)
playwright-report-trial/
test-results-trial/

import { ProductPage } from "@/components/pages/ProductPage";

// MaxClaw (STORY_025): the reference's product page, rendered inert.
export default function MaxClaw() {
  return <ProductPage product="max-claw" />;
}

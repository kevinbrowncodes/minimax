# Agent fixtures (STORY_048)

Real replies from `gemini-3.8-flash` on 2026-09-17, saved by the spike (`spark/gcloud/spike.sh`), for STORY_049's format check and the stub's fake Vertex to use as text — never a photo, never a key.

| File | What it is |
| --- | --- |
| `office-expanded.txt` | the office photo, skill v1.1, the two-pass shape (draft 241 words → 491): passes the skill's checklist |
| `cove-expanded.txt` | the cove photo, two-pass with thinking LOW (267 → 418 words): passes |
| `office-draft.txt` | the office photo, one pass, skill v1.1 (238 words): the shape the model writes unprompted — under the skill's floor |

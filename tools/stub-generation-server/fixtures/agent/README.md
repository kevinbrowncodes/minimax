# The fake Vertex's replies (STORY_049)

Real replies from `gemini-3.8-flash` on 2026-09-17, saved by STORY_048's spike, served by the stub's fake `generateContent` by script name (`src/agent.ts`). Text only — never a photo, never a key.

| File | Served for | What it is |
| --- | --- | --- |
| `draft.txt` | the first pass of `clean` and `warn` | the office photo, one pass (238 words) — the shape the model writes unprompted |
| `clean.txt` | the second pass of `clean` | the office photo expanded (491 words): passes the format check |
| `chain.txt` | both passes of `chain` | the first three segments of the six-segment chain reply at thinking LOW: segment 1 with the instruction line, segments 2–3 at the marker |

`warn` is `clean.txt` with the soundscape field dropped and the description cut to ≈ 300 words, made at runtime; `refusal`, `refusal-text`, `malformed`, `slow`, `quota` need no text file.

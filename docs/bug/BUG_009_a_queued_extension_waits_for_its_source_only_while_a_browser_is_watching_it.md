# BUG_009 — A queued extension waits for its source only while a browser is watching it

**Status:** Resolved (2026-09-16 11:15 EDT) — approved ≈ 10:15 EDT — the owner: "Yes please fix bug 009"; taken by session minimax-c6; the first draft of this line said 05:35, written from the session's clock rather than the wall clock — corrected)
**Found:** 2026-09-16 03:50 EDT, on the Spark, in the owner's overnight run (two 30 s chains queued up front through STORY_043; `test/26-09-18-2000_br/run-2026-09-15.md`)

## Summary

STORY_043 holds an extension in the line until its source is done, and STORY_042's ticker runs the runner every 30 s with no browser open. But the runner learns that a source is done **only from history**, and the only thing that writes a job's status into history is the task page's poll. With no browser on the source's task page, a waiting extension never goes. Overnight the GPU sat idle for 5 h 21 min (22:02 → 03:23 EDT) with a finished source and its extension "Waiting"; the extension went within 30 s of the owner opening the UI.

## Steps to reproduce

1. On the Spark with the model up, send a 10 s clip; on its task page choose Extend, +10 s, Send — Scheduled reads "Waiting · after <stamp>".
2. Close every browser tab. Watch `docker logs -t minimax-adapter`.
3. The source's `done` line appears (≈ 50 min). No `submitted` line follows for the extension — not after 30 s, not after an hour. GET `/api/queue` (from curl) still lists it as waiting.
4. Open the source's task page. Within 30 s the extension is submitted.

Observed 2026-09-15/16 three times in one run: chain 1's segment 2 went 42 min late (19:34 EDT after an 18:52 finish, when the owner opened the UI), segment 3 went 6 h 42 min late (03:23 after 20:40), chain 2's segment 2 went 5 h 45 min late (03:47 after 22:02). The Run-at path was unaffected: chain 2's first segment, held with `notBefore`, was submitted at 21:12:23 for 21:12:02.

## Expected vs actual behaviour

- **Expected:** the ticker submits a waiting extension within one tick of its source finishing, browser or no browser — that is what STORY_042 ("the queue runs with no browser open") and STORY_043 ("goes the moment its source is done") together promise, and what the README's Scheduled line says.
- **Actual:** it is submitted within one tick of the source's status being *polled*, which only a task page does.

## Root cause (read from the code, 2026-09-16)

- `app/lib/queue-runner.ts` › `submitDue` decides what is due with `sourceState(id)`, which reads `historyStore().get(id).status` and nothing else (lines 42–48, 69).
- The status in history is written by `historyStore().recordStatus`, whose only callers outside the runner are `GET /api/jobs/[id]` (the task page's poll) and `DELETE /api/jobs/[id]` (`app/app/api/jobs/[id]/route.ts` lines 26, 38, 45). Neither the ticker nor `GET /api/queue` nor `GET /api/history` asks the adapter anything.
- STORY_043's verification passed because its e2e and the real-chain check both had a page polling the source; its e2e cannot see this because a Playwright page is always polling.

## Acceptance criteria

- [x] The runner refreshes the status of every source that a waiting extension depends on from the model server before deciding what is due — one `GET /jobs/:upstreamId` per pending source per tick, recorded into history through the same path the task page uses — so a source that finished is seen within one tick with no browser open.
- [x] A source the model server no longer knows (404) is treated as gone, and the extension fails with "its source did not finish", as STORY_043 already specifies for a failed or cancelled source.
- [x] Unit (`queue-runner.test.ts`): with a fake model server whose source job answers `done`, a waiting extension is submitted on the next `submitDue()` with no prior status poll; with the source still `running`, it is not; with 404, it is failed and leaves the line. The refresh is skipped for entries with no `continueFrom` and for sources already terminal in history (no request made).
- [x] Integration (`api/queue` against the stub): a source submitted with `slow-done-after-10-polls`, an extension queued behind it, **no** `GET /api/jobs/:id` issued by the test; after the stub's job completes, the next `submitDue()` submits the extension; the stub records one create for it.
- [x] E2E: `queue.spec.ts` gains a case that closes the page after queuing the extension (or never opens the task page), waits on the stub's create request for the extension instead of on a page poll, then reopens Scheduled and asserts the row is Running. The existing STORY_043 cases stay green.
- [x] Manual verification on the Spark (not a gate): a 5 s clip and a +4 s extension queued together, every tab closed, `docker logs -t minimax-adapter` shows the extension submitted within 30 s of the clip's `done` line.

## Resolution (2026-09-16)

**Landed** (`db9a01d`, deployed 10:36 EDT): `lib/queue-runner.ts` › `refreshPendingSources({ refresh, staleMs, now })` runs at the start of every `submitDue` — for every source a waiting extension depends on (once per source per run; only sources that were submitted, are not terminal in history and have not been heard from within the staleness window) it asks the model server `GET /jobs/:upstreamId` and records the answer through `historyStore().recordStatus`, the path the task page's poll uses; a 404 fails the source with `not_found` "the generation server no longer knows this job", and the run's existing gone-source loop then fails its extensions with "its source did not finish"; an unreachable server or a non-JSON body leaves history as it was. `HistoryEntry.statusAt` is stamped on every `recordStatus`, and the runner asks only about sources whose stamp is older than `QUEUE_SOURCE_STALE_MS` (default 10 s) — a page watching a job polls it every 1–5 s, so a watched job costs the adapter no extra request, and an unwatched one is asked once per 30 s tick. The gate's Playwright server sets the window to 3 s and the integration tests 0. The README's Scheduled line says so.

**One refinement to the first acceptance criterion, made on the way:** "one GET per pending source per tick" holds for the case the bug is about (no browser); with a page polling the source the runner skips it, so the adapter is not asked twice for the same job every two seconds. The fifth criterion names `queue.spec.ts`; the queue's e2e cases live in `scheduled.spec.ts` and the new case went there.

**Tests:** `queue-runner.test` (a running then done source asked under the server's id and recorded, the extension going in the same run; a fresh source, a settled one, one still in the line and a fresh request never asked; two extensions of one source one question; a 404 failing the source and its extension; a rejected refresh and a 502 leaving history as it was; the env override), `history-store.test` (`statusAt` stamped when heard), `test/integration/queue.test` (a `done-after-1-poll` source and its queued extension with **no** `GET /api/jobs/:id` from the test: the list polls alone bring the source to done and submit the extension once, the stub's received request naming the source; the stub reset so the source answers 404 → the source failed `not_found`, the orphan failed `source_failed`), `e2e/scheduled.spec` "a waiting extension goes when its source finishes with no page watching it — the runner asks the stub itself" at desktop and narrow (the two requests through the route with no page open; `GET /api/history` called every 3.5 s as the ticker's stand-in — the ticker is off under Playwright and that route runs the same runner and never polls a job; the stub's job list grows to two; the first page opened shows the extension past Waiting and finishes it). Gate: typecheck, lint, unit, integration (31), build + image, e2e 105 passed / 9 skipped — by hand for the lanes touched, then all six in the pre-push hook (417 s).

**Manual verification (the Spark, 2026-09-16 10:36 → 11:15 EDT, MiniMax-H3 FL2VA `int8_convrot` through the adapter 1.4.0, ComfyUI v0.35.1 up, both peer sessions told before and after; the app container on `db9a01d`, its log reading `[queue] ticker started: every 30000 ms`).** At 10:36:28 a 5 s text-to-video prompt went through `POST /api/jobs` (straight to the adapter) and a +4 s extension of it at overlap 39 went through the same route → 202 queued, position 1. **From that second on nothing asked the app anything** — no page, no curl: the watcher read only the adapter's job endpoint and the queue and history files inside the container. The adapter's log: the clip **done at 10:53:32.837**; the extension **submitted at 10:53:33.014**, 177 ms later. The app's history shows the clip `done` with its result and `statusAt 10:53:33.003` — written by the runner's refresh: no page opened either task (`openedAt` unset on both), nothing ever polled the extension (its own entry still read `queued` at 11:15, so no Scheduled page was open either), and the app's log shows nothing. **Which caller ran the runner at that instant is not settled:** a run-at probe sent right after the check (submitted by the ticker at 11:17:49.794 and cancelled at 2 %, the entry removed) puts the ticks at ≈ :19 and :49 of each minute, so 10:53:33 was not a tick — a `GET /api/history` from outside this check (a home page, a curl) must have run the same code; before the fix that route asked the adapter nothing and would have submitted nothing, so the run still shows the fix and nothing else doing the work, but the clock bound the fix promises — within one 30 s tick — was not exercised here (the next tick, 10:53:49, would have done it 16 s after the done line). The tick path is proven by the unit test, the integration case and the e2e case, none of which polls the source. A second run on the Spark with the routes closed off is the way to see a tick do it, if wanted. The extension finished at **11:15:09** (21 min 36 s; the adapter's seam measure: no shot change). The same queue file still holds the overnight evidence the ticket was written from: chain 1's third segment created 18:02:02 and submitted 03:23:07. Overnight before the fix: 5 h 21 min idle on this; after it: 177 ms.

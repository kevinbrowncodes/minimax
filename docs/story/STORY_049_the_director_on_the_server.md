# STORY_049 — The director on the server

**Epic:** [EPIC_009](../epic/EPIC_009_agent_mode_a_director_writes_the_prompt_from_the_photo.md) — the third story; the route every UI story calls. After [STORY_048](STORY_048_the_gemini_spike.md) (the spike, whose request shape, model id, safety settings and refusal wording this story codes against)
**Status:** Proposed (2026-09-17 03:20 EDT — drafted with 048–055 at the owner's request; from EPIC_009 as revised in aa7092d; nothing built)
**Created:** 2026-09-17

As the owner, I want the app's own server to take a photo, a director skill and my notes, ask Gemini for the finished prompt, check the reply against the format the model on the Spark expects, and hand back either the prompt with anything it found wrong, or the model's refusal in its own words — so that the UI stories have one route to call and the gate can prove every outcome without ever reaching Google.

## Current state (read from the code, 2026-09-17 03:00 EDT)

- **The spike proved the call** (STORY_048): `lib/vertex.ts` signs, exchanges and posts; `lib/agent-request.ts` › `readSkill` and `assembleRequest` build the request; the model id is `VERTEX_MODEL`'s code default; the safety and generation settings are named constants. Nothing calls them from a route.
- **Routes and their tests.** Every API route is a `route.ts` under `app/app/api/…` wrapped in `guarded()` (`lib/model-client.ts`: config errors → 500 `config`, the contract's `{ error: { code, message, field? } }`); the integration lane (`app/test/integration/*.test.ts`) imports the handlers and starts the stub in-process (`createStubServer` from the `stub-generation-server` package, a random port, `MODEL_BASE_URL` set per test). `POST /api/jobs` reads multipart with `request.formData()`, takes `referenceImage` files and validates them with `lib/upload-validation.ts` (PNG / JPEG / WebP, ≤ 10 MiB, at most two).
- **The e2e lane picks the stub's outcome by name**: the composer forwards a `?script=` on the page URL to `POST /api/jobs?script=`, the route sends it upstream, and the stub reads it as `x-stub-script` or `?script=` (`server.ts` line 322; `isScriptName` refuses an unknown name). The stub's hooks live under `/__stub/`: `reset`, `busy`, `jobs`, `jobs/:id/received`, `fixtures/*`.
- **Skills at runtime.** `agents/skills/` is not in the production image — `app/Dockerfile`'s runner stage copies `.next/standalone` and `.next/static` and nothing else; the e2e server's working directory is `app/` (`playwright.config.ts` runs `node .next/standalone/app/server.js`). The owner's decision (2026-09-17): the folders are **baked into the image**, with a `SKILLS_DIR` for the gate and dev.
- **The base format, as the adapter reads it** (`spark/adapter/src/prompt.ts`): a prompt that starts with `integrated_multimodal_description:` or with either instruction line (`For the target video`, `How the reference pictures align`) passes through unchanged (`isBaseFormatPrompt`); `descriptionOf` is the text between the marker and `overall_soundscape:`. Nothing in the app checks a prompt's shape today; the adapter never refuses one.
- **Runs that end without a prompt have nowhere to live**: the Inbox (`lib/inbox.ts` › `eventsFor`) is a projection of history entries by task; a refusal that never became a job would be lost on reload. The owner's decision (2026-09-17): a small **agent-runs store** the Inbox merges in — the store is this story's (server side), the merge and the notice are STORY_050's.

## UI Mockup

UI Mockup: N/A (no UI change) — two routes, a checker, a store and the stub's fake Vertex; STORY_050 puts a face on them.

## Acceptance Criteria

- [ ] **`GET /api/agent/skills`** answers `{ skills: [{ id, name, description, metadata }] }` — one entry per folder under `SKILLS_DIR` that holds a `SKILL.md` with valid frontmatter, sorted by id; `id` is the folder name, `metadata` the frontmatter's map as strings. A folder with a broken `SKILL.md` is skipped and named in the server log once. `SKILLS_DIR` defaults to `/srv/agents/skills` in the image (the Dockerfile's runner stage copies `agents/` there) and to the repo's `agents/skills` resolved from the app's working directory otherwise; `playwright.config.ts` and the integration lane pass it explicitly.
- [ ] **`POST /api/agent/runs`** (multipart: `skill` — an id from the list; `referenceImage` — exactly one file, validated as the jobs route validates one; `notes` — text, may be empty; `?script=` forwarded upstream as the jobs route forwards it) assembles the request with `assembleRequest` (the skill's body as the system instruction, its references, the image, the notes — STORY_048's order), calls the model with the spike's safety and generation settings (`DIRECTOR_SAFETY_SETTINGS`, `DIRECTOR_GENERATION_CONFIG`; **the thinking level from `VERTEX_THINKING` — `low` by default, `medium`, `high` or `default` for the model's own choice — read from the env store then `process.env`, the owner's decision of 2026-09-17 after the spike's comparison**) in **two passes** — the draft, then the draft sent back with `expandInstruction(450, 600)` (which also asks for the subject to be named by appearance in every beat) — and the request's own `signal`, and answers **200** with one of: `{ kind: "prompt", prompt, findings }` — the reply's text trimmed, `findings` the format check's list (empty when clean); or `{ kind: "refusal", message }` — the model's words verbatim when it gave any, else the block or finish reason in a sentence ("The model stopped for safety: <category>"). Errors are the contract's shape: **400** `validation` (an unknown skill, a missing or rejected image); **503** `not_configured` with `readAgentConfig`'s reason; **502** `unreachable` (a network failure, the message without the URL); **429** `quota` relayed with Google's message; **502** `bad_reply` when the reply has **none** of the three fields (its first 200 characters in the message — the owner sees what came back); **499**-style `aborted` is never sent: an aborted request has no client. A run takes at most `AGENT_TIMEOUT_MS` (default 90 s), then `504` `timeout`.
- [ ] **The format check, `lib/prompt-format.ts` › `checkPromptFormat(text, { chain?: boolean })`** — pure, returns `{ findings: [{ code, message, segment? }], segments }`. For a single-clip prompt the codes are: `text-before-instruction` (anything before the instruction line — commentary, a title, a code fence), `no-instruction-line`, `no-description`, `no-soundscape`, `no-music`, `description-line-breaks` (a blank line or a label inside the description), `description-too-short` (< 350 words), `description-too-long` (> 600), `timestamps` (a `[m:ss` bracket, or the format's own cut phrase — a clock time followed by "the camera cuts" — inside a single-clip description), `markdown` (a fence or a heading anywhere), `no-shot-label` (`[Shot 1]` missing). Every message is plain English with the number it measured ("the description is 312 words; the skill asks for 350–600"). A code fence around an otherwise clean prompt is **stripped before checking** and reported as `markdown` so the prompt is still usable. The chain option is STORY_053's and is refused here (`chain: true` → the single-clip rules on segment 1 only — the rest defined there).
- [ ] **The reply is the prompt, findings and all** — the route never withholds a reply that has at least one of the three fields; the review-versus-block decision is the caller's (STORY_050 warns, STORY_051 blocks straight-through) — the owner's rule of 2026-09-17 (his answer to review question 7).
- [ ] **A run that ends without a prompt is recorded**: `lib/agent-run-store.ts` › `agent-runs.json` beside `history.json` (the env and skills stores' pattern, written atomically) keeps `{ id, at, skill, notes, outcome: "refusal" | "error", message }` for every refusal, `bad_reply`, `quota`, `unreachable` and `timeout` — **never for a prompt, never for an abort** (the owner's own act; the epic's list said Stop, corrected here and said so); `GET /api/agent/runs` lists them newest first with an optional `openedAt`; `PATCH /api/agent/runs/:id { openedAt }` stamps one (STORY_050's Inbox row uses it); the store keeps the last 200. No image is stored — the notes and the outcome only.
- [ ] **The stub gains a fake Vertex** (`tools/stub-generation-server`), on the stub's own port so `VERTEX_BASE_URL` and `VERTEX_TOKEN_URL` both point at it in the gate: `POST` on the token path returns a fake bearer token for any assertion; `GET` on the publisher-models path returns two Gemini entries; `POST …:generateContent` answers by script name (`x-stub-script` or `?script=`, default `clean`): **`clean`** — a committed fixture prompt for the stub's own reference image (`fixtures/agent/clean.txt`: the instruction line, a 350–600-word single paragraph, both sound fields — passes the check); **`warn`** — the same with the soundscape field dropped and the description cut to ≈ 300 words (two findings); **`refusal`** — a candidate stopped for safety with no text; **`refusal-text`** — a polite refusal in prose, in the shape the spike recorded; **`malformed`** — "I can't see an image in this request."; **`slow`** — `clean` after 8 s (Stop's test; an abort is noted); **`quota`** — 429 in Google's error shape. `GET /__stub/agent/runs` lists what the fake received — the script, whether the request was aborted, the system instruction's word count, the parts in order as `{ kind: "text", head }` (the first 60 characters) or `{ kind: "image", mimeType, bytes, sha256 }`, the safety settings — and `POST /__stub/reset` clears them. The fake's request parsing follows the shape STORY_048 recorded; nothing in it is Google's code.
- [ ] **The gate never calls Google**: `playwright.config.ts` passes `VERTEX_BASE_URL`, `VERTEX_TOKEN_URL` (the stub), `VERTEX_PROJECT`, `VERTEX_LOCATION`, `VERTEX_MODEL`, `SKILLS_DIR` and `GOOGLE_APPLICATION_CREDENTIALS` — **a key pair generated in `globalSetup` into the run's temp dir**, never a committed key — to the built app; the integration lane does the same per test. A source-text guard asserts no test file and no fixture contains a private-key block.
- [ ] **The image goes to Google as the route received it** — no image library on the server (zero runtime dependencies, and Node has no canvas). If STORY_048 found downscaling worth it, the **composer** does it in the browser with a canvas before posting (STORY_050's job, noted there); the route never resizes.

## Departures from the reference

- None — no UI surface.

## Technical Notes

- `app/app/api/agent/skills/route.ts`, `app/app/api/agent/runs/route.ts`, `app/app/api/agent/runs/[id]/route.ts`; `lib/agent-config.ts` gains `skillsDir`, `vertexBaseUrl`, `tokenUrl`, `timeoutMs`, `thinking` (from `VERTEX_THINKING`, validated to the four values, `low` when unset or unknown; `.env.example` documents it); `lib/vertex.ts` gains an in-memory token cache (`tokenFor(config)`: reused until a minute before expiry, refetched after; one in flight at a time).
- The abort: the route passes `request.signal` to `generateContent`'s `fetch`; an aborted run writes nothing and returns nothing (Next drops the response).
- `lib/prompt-format.ts` reuses the adapter's vocabulary by value, not by import (the packages are separate): the marker, the two instruction starts, the soundscape and music markers, `[Shot 1]`, `FPS`-free. Word count = whitespace-split tokens of the description with the `[Shot 1]` label removed.
- The Dockerfile: `COPY --chown=node:node agents/ ./agents/` in the runner stage and `ENV SKILLS_DIR=/srv/agents/skills`; `.dockerignore` unchanged (nothing under `agents/` is heavy or secret).
- The stub: `src/agent.ts` (the fake's handlers and scripts), `src/agent.test.ts`; `fixtures/agent/*.txt`; `README.md` gains the fake's section. The `StubScript` union in `app/e2e/fixtures/stub.ts` gains a sibling `AgentScript` and a `received.agent()` helper (STORY_050's specs read it).
- `.env.example`: `VERTEX_BASE_URL` and `VERTEX_TOKEN_URL` as gate-only knobs, `AGENT_TIMEOUT_MS`, `SKILLS_DIR`.

## Testing Plan

- **Unit (`pnpm test`)** — `lib/prompt-format.test.ts` (new): clean on the stub's `clean.txt` and on the office and cove prompts as the spike recorded them (committed under `app/test/fixtures/agent/` as text); each finding code on a prompt broken for it (a title before line 1; no instruction line; the description with a blank line; 312 words; 640 words; a `[0:03-` bracket; "At 00:03.500, the camera cuts"; a fence around the whole thing — stripped and reported; `[Shot 1]` missing); several findings at once, in document order. `lib/agent-run-store.test.ts` (new): record, list newest first, the 200 cap, `openedAt`, atomic write, garbage on disk → empty. `lib/agent-config.test.ts` (047's, extended): the new fields and their defaults. Stub: `src/agent.test.ts` — every script's response shape, the token path, the models path, the received record (parts, image sha256, abort noted), reset.
- **Integration (`pnpm test:integration`)** — `test/integration/agent.test.ts` (new), the stub in-process, a generated key in a temp file, `SKILLS_DIR` at the repo's folder: the skills list names the thirst-trap folder with its six metadata keys; a run with the fixture image and notes → `prompt` and `findings: []`, and the fake's record shows the system instruction, three reference parts, the image (sha256 equal to the fixture's), the notes last; `warn` → two findings with their messages; `refusal` and `refusal-text` → `kind: "refusal"` with the words, and one row in `agent-runs.json`; `malformed` → 502 `bad_reply` with the text, recorded; `quota` → 429, recorded; `VERTEX_BASE_URL` at a closed port → 502 `unreachable`, recorded, the URL absent from the message; `VERTEX_PROJECT` unset → 503 `not_configured`; an unknown skill and a `.gif` → 400 with the field; an `AbortController` aborted mid-`slow` → the fake's record says aborted and the store has no row; `GET /api/agent/runs` lists the rows newest first; `PATCH …/:id` stamps `openedAt`. `capabilities.test.ts` (047's): `agent.configured` true with the pinned model.
- **E2E** — none: no UI (STORY_050's `agent.spec.ts` drives these routes through the composer at both widths). Said so as § 3.5 asks.
- **Manual verification (the Spark, in the Done note with the date):** the deployed app's `GET /api/agent/skills` and one `POST /api/agent/runs` with `curl` from the Spark using the office photo — the reply's finish reason, token counts and findings in the Done note; the notes-only run (no image) refused with 400. No GPU.

## Estimated Complexity

Medium — two routes and a store on patterns the app already has, one pure checker with a dozen rules, and the fake with its scripts and hooks: ≈ 1 h 30 min of build and gate (the epic's figure); no GPU.

# STORY_048 — The Gemini spike

**Epic:** [EPIC_009](../epic/EPIC_009_agent_mode_a_director_writes_the_prompt_from_the_photo.md) — the second story; the spike every later story codes against. After [STORY_047](STORY_047_the_google_credential.md) (the credential)
**Status:** Done (2026-09-17 10:45 EDT — approved under the owner's blanket "continue story by story" of 05:20; built 05:25 → 06:20 (the client, the assembly, the CLI, the tests), 17 calls to Google 10:00 → 10:44 UTC−4 with the owner's say on the re-runs and the two-pass probe; gate steps 1–4 green by hand at 10:47, build + e2e after; deployed and verified live; the Done note and [the replies file](STORY_048_the_gemini_spike_replies.md))
**Created:** 2026-09-17

As the owner, I want the thirst-trap director skill run against Gemini Flash on my own project with the three photos I have already directed by hand, from a script that lives in the repo, so that before any UI is built we know which model to pin, what a request and a reply look like, what a refusal looks like, what a call costs, and whether the skill's wording needs tuning — all written down with the date.

## Current state (read from the code, 2026-09-17 03:00 EDT)

- **The skill** — `agents/skills/minimax-h3-director-thirst-trap/SKILL.md` (1,985 words) with `references/base-en.md` (2,536), `references/example-i2va.md` (884) and `references/anchor-example.md` (374): ≈ 5,800 words in all, ≈ 8k tokens by the usual ratio. Its output opens with the I2VA instruction line verbatim, then `integrated_multimodal_description:` (one paragraph, 350–600 words, no timestamps), `overall_soundscape:` and `non_diegetic_music:`; its **Checklist** section is the format's own definition. It was written for a reader that can view the image — the assistant, in the cove round (4 of 4 held) and the office round (4 of 4 real draws held), both 2026-09-16.
- **The photos** — the ones the skill has been run on by hand: the studio (`spark/data/input/01.jpg`, the owner's, gitignored), the cove (`test/26-09-16-0500_cove/01.jpeg`, 1376×768) and the office (`test/26-09-17-0800_office/01.jpeg`, 1376×768); the bathroom (`test/26-09-18-2000_br/01.jpeg`, 1280×720) is the fourth on disk. `test/` is untracked and stays so. The hand-written prompts for the cove and the office are beside them (`prompt.txt`, the round's `run.md`), so a reply can be compared with what held on the box.
- **The credential and the config** — STORY_047's: the key at `/secrets/vertex-sa.json` in the app container, `lib/agent-config.ts` › `readAgentConfig()` reading `VERTEX_PROJECT`, `VERTEX_LOCATION`, `VERTEX_MODEL` from the env store then `process.env`. After 047 the model is unset by design ("`VERTEX_MODEL` is not set — STORY_048's spike pins it").
- **No HTTP client but `fetch`, no crypto but `node:crypto`** — the app's runtime dependencies are Next and React (`app/package.json`); the adapter's are none. The token for a service account is an RS256-signed JWT (`createSign("RSA-SHA256")`) exchanged at Google's OAuth2 token endpoint for a bearer token — no SDK.
- **Node runs TypeScript** — the gate image is `node:26` (`compose.yaml` › `minimax/gate:1.63.0-node26`), which strips types from a `.ts` entry point without a build step, so a script under `app/scripts/` runs as `node app/scripts/agent-spike.ts` inside the gate container with the repo bind-mounted at `/work`.
- **The gate never calls Google** (CLAUDE.md § 4a) — this story's calls are a manual verification and never a test; the fake Vertex is STORY_049's.

## UI Mockup

UI Mockup: N/A (no UI change) — a script, a library module and a report.

## Acceptance Criteria

- [x] **`lib/vertex.ts`** (app, pure apart from `fetch` and `node:crypto`, every network call through an injected `fetch`): `serviceAccountToken(key, { tokenUrl, fetch, now })` builds the RS256 JWT (`iss` = the key's `client_email`, `scope` = the cloud-platform scope, `aud` = the key's `token_uri`, `iat`/`exp` one hour) and exchanges it for `{ accessToken, expiresAt }`; `listPublisherModels(config, token, fetch)` returns the Gemini models the project's region offers, name and display name; `generateContent(config, token, request, fetch, signal?)` posts to the model's `generateContent` endpoint under `VERTEX_BASE_URL` (the real host by default — the exact URL and body shape **read from the Vertex docs the day this is built and copied into the module's header with the date**) with a system instruction, an ordered list of parts (text, or inline image data with its MIME type), the safety settings and the generation config it is given, and returns `{ kind: "reply", text, finishReason, usage: { promptTokens, candidateTokens, totalTokens } }`, `{ kind: "refusal", finishReason, message }` for a candidate stopped for safety or a prompt blocked before generation (the model's own words when it gave any, else the block reason), or `{ kind: "error", status, message }` for an HTTP failure — the response body's error message kept, never the URL or the token. The key file is read once by the caller; nothing in the module logs.
- [x] **`app/scripts/agent-spike.ts`**, run as `pnpm --filter app agent:spike <skill-dir> <image> [notes…]` inside the gate container (`tools/gate/run.sh`'s image, the repo at `/work`, the key mounted read-only by a `gate` volume entry added for it): reads the skill folder (frontmatter off, the body as the system instruction; each `references/*.md` a text part headed by its file name; the image an inline part; the notes the last text part, or "No notes." when none) — **the assembly is a pure function, `lib/agent-request.ts` › `assembleRequest(skill, image, notes)`, that STORY_049's route reuses unchanged** — calls the model, and prints to stdout only: the model id, the finish reason, the token counts, the latency, and the reply verbatim between markers. A `--models` flag prints the publisher-models list for the region instead. A `--safety least` flag sends the least restrictive thresholds the docs allow; the default sends none. Nothing it prints is a secret; the key's path comes from `GOOGLE_APPLICATION_CREDENTIALS`.
- [x] **The model is pinned from the list, not from memory.** The Done note records the publisher-models list as returned (names and display names of the Gemini Flash entries, the date), which one is the newest Flash, and whether it is the one the owner calls "Flash 3.8". That id becomes the **code default** of `VERTEX_MODEL` in `lib/agent-config.ts` (an env value still overrides it), so after this story `/api/capabilities` › `agent` reads `configured: true` on the Spark.
- [x] **Three photos, two safety settings each** — the studio, the cove and the office, first with the default thresholds and again with the least restrictive the docs allow (the Done note says which categories accepted the setting and which need an allowlist and were left at their default): every reply and every refusal **verbatim** in the Done note, with the finish reason, the token counts (prompt / candidates / total, and the image's share where the usage reports it), the latency, and the cost of the call from Google's price list read that day (the page's URL and the date beside the figure). Six calls, plus the model list; no more without the owner's say.
- [x] **Each reply is read against the skill's own Checklist**, item by item, in a table in the Done note: line 1 the instruction line verbatim and nothing before it; the description one paragraph of 350–600 words with no labels, line breaks, timestamps or bracketed times; `[Shot 1]` first, then style, then camera with the single-shot declaration; everything visible named; the out-of-frame reveal described; the subject named by appearance each time; first seconds → middle → final → hold; both sound fields, `None.` unless asked. For the cove and the office, the reply is set beside the hand-written prompt that held on the box (`test/…/prompt.txt`) and the differences of substance noted (what the model saw that the hand missed, and the reverse). **If a reply misses an item, the skill's wording is tuned in a CHORE** filed from this story (the skill's `metadata.version` bumped, validated with `agentskills validate` from a container as CHORE_012 did) and the photo re-run once — counted against the six.
- [x] **The image question is answered by measurement**: the office photo sent at its 1376×768 and once more downscaled to 768 px wide (the script's `--width 768`), the two prompt-token counts and the two replies' checklist results side by side; the Done note says whether STORY_049 downscales (a token saving worth having, or none).
- [x] **Context caching is decided by arithmetic, not built.** From the price list read that day: the per-token input price, the cached-token price, and the cache storage price per hour; the skill + references' measured token count; the break-even in calls per hour written out. The Done note's verdict — worth a later story, or not at this usage — goes into the epic's table; 049 ships without caching either way (the owner's answer of 2026-09-17 00:50).
- [x] **The README's Agent mode section** (047's) gains: the model id and the date it was pinned, the price per call with its source and date, the safety settings the app will send and why, the caching verdict, and the sentence that every photo and note goes to Google. **Nothing else changes in the app**: no route, no UI.

**AC corrections made during implementation, as § 3 item 8 asks (2026-09-17):**

- **AC 1 and 3, "listPublisherModels" / "pinned from the list":** Vertex has no public list method under `publishers.models` — the v1 discovery document (revision 20260904) lists `get`, `generateContent`, `countTokens` and the predict methods. The client has `getPublisherModel` instead, and the spike's `--models` runs `get` on the Flash ids Google's model page names (its navigation; the page body is client-rendered). Result: **`gemini-3.8-flash`, `3.7`, `3.6`, `3.5`, `2.5` and the two Flash-Lites are GA; `gemini-3-flash` is not a model.** The newest, `gemini-3.8-flash`, is pinned as `DEFAULT_MODEL`.
- **The location:** `us-central1` answers 404 for `gemini-3.8-flash` on this project ("not found or your project does not have access … in the specified region"); the model is served from **`global`**, which is also the cheaper price row. `DEFAULT_LOCATION`, `.env`, `.env.example`, the compose defaults and `setup-vertex.sh`'s default region are now `global`; the client uses the bare host for it.
- **AC 6, "downscaled to 768 px wide":** Node has no image library and the app takes none; the API's own `mediaResolution` is the knob. Measured: LOW cuts the image from 1,100 to 264 tokens (the prompt 9,270 → 8,434, − 9 %), HIGH equals the default. Not worth changing: the image is 12 % of the prompt and the text is the rest. 049 sends the default.
- **AC 2, `assembleRequest`:** gained `followUp` on the client and `expandInstruction` in the assembly — the **two-pass shape** (below), which the route adopts; and the director's settings are exported as constants (`DIRECTOR_SAFETY_SETTINGS`, `DIRECTOR_GENERATION_CONFIG`, `DIRECTOR_EXPAND_WORDS`) so 049 sends what the spike proved.
- **The call count:** six planned plus the media pair; the owner said yes to three re-runs on the tuned skill and to the two-pass probe; two more probes (the length demand in the user turn; the same with thinking off) were the assistant's, to explain a MAX_TOKENS on a complete reply before drawing conclusions — said here. 17 calls, ≈ 30 ¢.

## Departures from the reference

- None — no UI surface; agent.minimax.io's agent is its own model.

## Technical Notes

- `lib/vertex.ts`: `signJwt(claims, privateKeyPem)` with `createSign("RSA-SHA256")` over `base64url(header).base64url(claims)`; the token exchange is a `POST` of `grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer&assertion=<jwt>` as a form body; the expiry is kept so STORY_049 can cache the token in memory until a minute before it lapses. Types for the key file (`client_email`, `private_key`, `token_uri`) and the authorized-user shape 047 named as the fallback (`refresh_token`, `client_id`, `client_secret` → a refresh-token exchange at the same endpoint); `readKeyFile(path)` tells the two apart.
- `lib/agent-request.ts`: `readSkill(dir)` → `{ id, name, description, metadata, body, references: [{ file, text }] }` (frontmatter parsed by hand — three keys and a nested map; no YAML library); `assembleRequest(skill, image: { bytes, mimeType }, notes, instructions = [])` → `{ systemInstruction, parts }` with the order fixed here and reused by 049 and 052.
- The request's `generationConfig` (temperature, max output tokens) and `safetySettings` are constants the spike names and the Done note records; 049 imports them.
- `VERTEX_BASE_URL` (default the real regional host, read from the docs) and `VERTEX_TOKEN_URL` (default the key's `token_uri`) are read by `readAgentConfig()` so the stub can stand in for both in 049; documented in `.env.example` as gate-only knobs.
- The spike runs in the gate container so nothing is installed on the box: `docker compose run --rm gate pnpm --filter app agent:spike …` with the key volume; the photos are read from their paths on the bind-mounted repo (`test/…`, untracked) and from `spark/data/input/` (a second bind mount for the run, read-only).
- Google's price list and the Vertex request docs are read the day this is built (CLAUDE.md § 3.8's rule for live state); the module header and the Done note carry the URLs and the date.

## Testing Plan

- **Unit (`pnpm test`, app)** — `lib/vertex.test.ts` (new): the JWT's header and claims decoded and its signature verified with `crypto.verify` against a key pair the test generates (`generateKeyPairSync("rsa")` — no key file in the repo, ever); the token exchange's form body and the parsed `{ accessToken, expiresAt }` against an injected `fetch`; `generateContent`'s request body shape (the system instruction, the parts in order, the inline image's base64 and MIME type, the safety and generation settings) against an injected `fetch`; the three result kinds from canned response bodies — a reply with usage, a candidate stopped for safety with and without text, a prompt blocked before generation, a 429 and a 500 with Google's error JSON — and that no result string contains the token or the URL. `lib/agent-request.test.ts` (new): `readSkill` on the committed thirst-trap folder (name, description, the six metadata keys, the body without frontmatter, three references in name order); `assembleRequest`'s part order with and without notes. A guard that the spike script imports only from `lib/` and `node:` (the source-text-guard pattern) so the assembly cannot drift from the route's.
- **Integration** — none: no route changes. (Said so as CLAUDE.md § 3.5 asks; the routes are STORY_049's, tested there against the fake.)
- **E2E** — none: no UI.
- **Manual verification (Google, in the Done note with the date):** the model list; the six calls (three photos × two safety settings) with everything AC 4–5 lists; the downscale pair; the caching arithmetic; the price list's URL and date. The Spark is not used.

## Estimated Complexity

Medium — a signing-and-exchange client, the request assembly, a CLI, and a report that later stories read as their spec: ≈ 1 h 30 min of build and gate (the epic's figure, at 1.5× a Small for another provider's unknowns), plus the calls themselves (seconds each) and the reading of the docs and the price list; no GPU.

## Done note (2026-09-17)

**Built.** `app/lib/vertex.ts` (the key file's two shapes; `signJwt` with `node:crypto`; `fetchToken` — the JWT-bearer grant for a service account, the refresh-token grant for an authorized user; `generateContent` with system instruction, ordered parts, safety and generation settings, an optional follow-up turn, the request's signal; `parseGenerateResponse` → reply / refusal / error; `getPublisherModel`; the `global` host rule — every shape from Google's discovery document and OAuth page, cited in the header with the date), `app/lib/agent-request.ts` (`parseFrontmatter`, `readSkill`, `readSkills`, `assembleRequest` in the fixed order, `expandInstruction`, the director's constants), `app/scripts/agent-spike.ts` (the CLI: `--models`, `--safety`, `--media`, `--max-tokens`, `--thinking`, `--expand`, `--out`; paths from the repo root through `INIT_CWD`; the header saved with every run), `pnpm --filter app agent:spike` with `tsx` declared as the app's devDependency (already in the lockfile as vite's optional peer — no download), the `agent-spike` compose service (the gate image with the key mounted read-only — the `gate` service itself never sees the key) and `spark/gcloud/spike.sh`; `lib/agent-config.ts` pins `gemini-3.8-flash` and `global`. Tests: `vertex.test.ts` (12: the JWT verified with a key pair generated in the test, the two grants, the body shape with and without a follow-up, the reply / refusal / block / error parsing, the hosts, the abort), `agent-request.test.ts` (7: the committed skill's keys, body and references; the parser; the order with 0 / 2 instructions; the constants), `scripts/agent-spike.test.ts` (the import guard). Two of 047's tests asserted the "no model pinned" state this story ends and were rewritten to the pinned default (§ 4c: an implementation detail, not a behaviour anyone wants kept).

**Skill tuning:** [CHORE_014](../chore/CHORE_014_the_thirst_trap_director_states_its_length_rule_first.md) — v1.1 with the length rule first and the two keys 050 reads; validated from a container. It did not move the model (below).

**The model, from Vertex:** `publishers.models.get` with the service account's token, global, 2026-09-17 — `gemini-3.8-flash` GA (the owner's "Flash 3.8"), the newest Flash. Pinned.

**The 17 calls** — every one verbatim in [STORY_048_the_gemini_spike_replies.md](STORY_048_the_gemini_spike_replies.md); the office, cove and studio photos; `gemini-3.8-flash`, global:

| Finding | Evidence |
| --- | --- |
| **No refusal, at either safety setting, on any photo** — the studio posing photo included | runs 1–6: finish STOP ×6, no safety ratings returned at all; **`OFF` on all five text categories was accepted without an allowlist** (runs 4–6) — 049 sends OFF |
| **The format is right every time** | 15 of 15: the instruction line first, `[Shot 1]`, the three fields, no bracketed times, no fences, no commentary |
| **The length is not** — the model writes ≈ 250 words whatever it is told | v1.0: 184–246 words; v1.1 (the rule first): 226–251; the demand in the user turn: 279–282; thinking off: the same length. The anchors are ≈ 90 words at v1.0, ≈ 140 at v1.1 |
| **A second pass fixes it** — the model expands its own draft | run 14: 241 → **491** words; run 15: 267 → **418**; both one paragraph, every fact kept, the anchor at the skill's 150–250 (the two are committed as `app/test/fixtures/agent/*-expanded.txt` for 049's check) |
| **Thinking costs and adds nothing to the length** | run 14's second pass: 6,075 thinking tokens; run 15 with `thinkingLevel: LOW`: 0, the same shape → 049 sends LOW |
| **A 4,096-token cap is consumed by thinking** — a complete reply came back as MAX_TOKENS | run 12 (1,855 thoughts + 434 out under a 4,096 cap) → 049 sends 16,384 |
| **Tokens per call** | ≈ 9,270 in (image 1,100 at the default resolution, the skill + references 8,170), ≈ 400 out; the second pass ≈ 10,140 in, ≈ 650 out |
| **Latency is Google's and swings** | 6.9 s to 107 s for the same request shape, in one hour; median ≈ 15 s a pass |
| **Media resolution** | LOW: the image 264 tokens (− 9 % of the prompt); HIGH = default. Not used |

**Price, from `cloud.google.com/vertex-ai/generative-ai/pricing` read 2026-09-17:** Gemini 3.8 Flash, global, introductory through 2026-12-31 — **$0.75 / 1M input, $3.75 / 1M output (response and reasoning), $0.075 / 1M cached input**; from 2027-01-01 $1.50 / $7.50 / $0.15; context-cache storage $1.00 / 1M tokens / hour. Per run: one pass with default thinking ≈ 1.3 ¢; the two-pass shape with thinking LOW **≈ 1.9 ¢** (≈ 3.8 ¢ from 2027); the whole spike ≈ 30 ¢.

**Caching, by arithmetic:** the cacheable part is the skill + references, 8,170 tokens; a cache saves 8,170 × ($0.75 − $0.075) / 1M = 0.55 ¢ per call and costs 8,170 × $1 / 1M = 0.82 ¢ per hour to hold → **break-even ≈ 1.5 calls per hour, sustained**. At a handful of runs a day it costs more than it saves. Deferred, as the owner decided; revisit if usage passes ≈ 2 runs an hour.

**Gate.** Steps 1–4 by hand at 10:47 (typecheck, lint, unit 299 passed incl. the 20 new, integration 36); build + e2e by hand after (113 passed at both widths); the hook re-ran all six on the push.

**Verified live (2026-09-17):** the app rebuilt from this tree and restarted with `openJobs: 0`; `GET /api/capabilities` › `agent` → `{ "configured": true }` — the model pinned, the key mounted, the project and `global` from `.env`; `spark/gcloud/spike.sh --models` from the deployed setup lists the Flash models GA. `git status --short` shows nothing under `spark/data/`, no `.env`, no key; the fixtures are text.

**For 049:** send `DIRECTOR_SAFETY_SETTINGS` (OFF ×5), `DIRECTOR_GENERATION_CONFIG` (16,384; thinking LOW), the default media resolution; two passes (`assembleRequest` → the draft → `followUp` with `expandInstruction(450, 600)`); the format check's floor at 400 with the two-pass output in mind; the fake Vertex answers the two passes in turn.

**Addendum (2026-09-17 11:20 EDT) — the thinking level, measured at the owner's request.** The office photo, two-pass, one run per `thinkingLevel` (MINIMAL: "unsupported" on this model), scored against the skill's checklist and compared on the facts each anchor asserts — [STORY_048_thinking_levels.md](STORY_048_thinking_levels.md) has the scorecard and the four prompts in full. LOW: 522 words, a 290-word anchor, 1.9 ¢, 13 s a pass; MEDIUM 490 / 266 / 4.4 ¢ / 49 s; HIGH 471 / 245 / 6.9 ¢ / 63 s; default 484 / 260 / 3.4 ¢ / 29 s. The differences are within run-to-run variation; the four agree on every solid fact and disagree the same way on what the photo does not settle (the floor). HIGH named the subject by appearance three times to LOW's once — the one signal for thinking, one sample. **The owner's decision: LOW by default, configurable** — STORY_049 reads `VERTEX_THINKING` (`low` | `medium` | `high` | `default`) from the env store then `.env`, and the expansion turn asks for the subject to be named by appearance in every beat. The first Spark draw (STORY_050) is the real test; the flip is one setting.

**Addendum (2026-09-17 11:45 EDT) — six segments, at the owner's request ("my concern is the quality when we need 6 scripts").** STORY_053's chain director drafted early (`agents/skills/minimax-h3-director-thirst-trap-chain/`, v0.1, validated; the single-clip rules appended as its reference section) and run on the office photo with "6 segments" in the notes, two-pass with a chain expansion turn, at LOW, default and HIGH — [STORY_048_chain_thinking_levels.md](STORY_048_chain_thinking_levels.md) has the per-segment scorecard, every join's hold → opening pair, and the three replies in full. All three: six segments, segment 1 in the full format and segments 2–6 at the marker with `twelve-second` and no picture, **the same anchor word for word in all six**, both sound fields on each, 440–574 words per description. LOW: 4.4 ¢, 60 s; default: 7.9 ¢, 116 s, the richest carry-over at the joins (12–16 shared key words vs LOW's 8–11); HIGH: 11.1 ¢, 155 s, the weakest joins (3–7) and the one self-contradiction (a laminate desk that became mahogany). Not a ladder: LOW ≈ default > HIGH. **The owner's decision: LOW by default for single clips and chains alike, `VERTEX_THINKING` configurable upward**; the chain expansion turn now asks each opening to restate the hands, the weight, the gaze and any held object — the thing default did better.

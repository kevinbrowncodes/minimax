# STORY_041 — Scheduled is the queue of generations that run one after another

**Epic:** [EPIC_007](../epic/EPIC_007_a_queue_of_generations.md)
**Status:** Done (2026-09-15) — approved that day by the owner ("Approve, but add timed runs", then "Yes, add Edit to STORY_041"; the timed-run and Edit ACs and notes were added on those answers before any code); from [BACKLOG_008](../backlog/BACKLOG_008_scheduled_generations_run_one_after_another.md)
**Created:** 2026-09-15

As the owner, I want to send several generations in a row and see them run one after another — a Scheduled page that shows the queue in order, lets me reorder or remove what has not started, a Send that queues instead of failing when the Spark is busy, a "run at" time I can put on a queued job so it waits for the hour I choose, and an Edit that reopens a queued job in the composer — so that I can line up an evening's clips and collect them in the morning.

## Current state (read from the code, 2026-09-15)

The adapter accepts up to five open jobs (`maxOpenJobs`, default 5) and submits each to ComfyUI at once; ComfyUI runs prompts one at a time in submission order. The UI can already send while another job runs (the home composer; the task page's docked composer shows Stop generation instead), each new job appears in Recents as Queued then Generating, and the sixth prompt is refused with "The Spark is busy" (503 `busy`). There is no page that shows the queue as a whole, no position, no reordering, no way to remove a queued job but cancelling it. `/scheduled` answers 404 (STORY_026); the reference's page was the agent's timed tasks, which we do not have.

## UI Mockup

**Reference captures (the chrome only):** `page-scheduled@1440` / `-dark`, `narrow-page-scheduled@390` / `-dark` (2026-09-14): the "Schedules" heading, the "Search scheduled tasks" field with the "Scheduled task status" filter (All), **Create** top-right, the empty state "No scheduled tasks yet." with a Create button. The rows are ours.

```
sidebar: New task · Search · Plugins · Scheduled · Assets · Connect mobile          (the row is back, between Plugins and Assets)

Schedules                                                                  [+ Create]
[🔍 Search scheduled tasks                     ] [Status: All ▾ (All · Waiting · Running · Done · Failed)]
#  ─ Running ─────────────────────────────────────────────────────────────────────
1  ● 26-09-15-1901  Paper boat on rain puddle                 Generating 41 %      [Stop]
   ─ Waiting (3) ─────────────────────────────────────────────────────────────────
2  ○ 26-09-15-1902  Same boat, wider                          Waiting · next       [↑] [↓] [Run at…] [Edit] [Remove]
3  ○ 26-09-15-1904  Candle on a wooden table                  Waiting              [↑] [↓] [Run at…] [Edit] [Remove]
4  ○ 26-09-15-1905  Neon street at night                      Not before 02:00     [↑] [↓] [Run at…] [Edit] [Remove]
   (Run at… = a date-time field on the row; clear it to run at the next free slot; a timed row is skipped until its time and never blocks the ones behind it)
   (Edit = the home composer with the job's prompt, parameters, reference images and project as sent, "Editing a queued job" above it; Send replaces the entry in place — same position, same run-at — and Cancel editing leaves it as it was)
   ─ Done today ──────────────────────────────────────────────────────────────────
   ● 26-09-15-1730  Forest dawn fly-through                   Done 17:52           → the task page
empty: "No scheduled tasks yet."  + Create → the home composer
Create = the composer as it is: Send queues (a toast "Queued — 3rd in line" when the Spark is busy) and opens the task page, whose indicator reads "Waiting — 3rd in line" until the job is submitted
```

## Acceptance Criteria

- [x] **Send never fails as busy:** when the adapter answers 503 `busy` (or its open jobs are at the limit), the request is kept in the app's own queue (`/data/queue.json`, with the images it carries) as a history entry with status `waiting` and a position; the task page opens and its indicator reads "Waiting — Nth in line"; the queue runner submits the next waiting request to `POST /jobs` as soon as a slot frees (polled with the recents, and on every job's terminal status), in order; a submitted one becomes an ordinary job (its id changes from the queue's to the adapter's — the history entry keeps its original id and records the job's).
- [x] **The Scheduled page** at `/scheduled` (the sidebar row back, between Plugins and Assets, as the reference has it): Running (the adapter's open jobs, with progress and Stop), Waiting (ours, in order, with position, move up / down and Remove — no confirm; the entry is deleted), Done today / Failed (the last 24 hours, each a link to its task page); the search filters by title; the status filter as sketched; the empty state as captured; **Create** opens the home composer.
- [x] **Timed runs (the owner, 2026-09-15):** a waiting row can carry a "run at" time (`notBefore`, ISO, set and cleared on the page, and offered by the composer's Send as an optional "Run at…" beside Queue); the runner never submits it before that time and does not let it block the rows behind it (they go first); the row reads "Not before 02:00" (today) or "Not before Sep 16, 02:00"; the app container must be up for a timed job to go (the runner lives in it — the page says so under the field).
- [x] **Edit a waiting job (the owner, 2026-09-15):** a waiting row's **Edit** opens the home composer (`/?queue=<id>`) with the job's request as sent — the prompt, ratio / resolution / duration / model, the reference images (from the queue's copy of the files), the project, the run-at — under a line "Editing a queued job — Send replaces it, Cancel editing keeps it"; Send replaces the entry in place (same position and run-at unless changed) without ever creating a second job; Cancel editing leaves the entry untouched; a job that started while it was being edited cannot be replaced — Send says so and the composer keeps the text.
- [x] Cancel of a running job (the task page's Stop, the queue's Stop) frees the slot and the next waiting request goes; Remove of a waiting one never touches the adapter.
- [x] The Inbox (STORY_033) gets the same events as today when a queued job finishes; the queue survives an app restart (the file) and a job that the adapter no longer knows is marked failed with the reason.
- [x] Both widths (the page at 390 with the bar's Search as Assets has it), both themes; the STORY_026 e2e sweep is updated: `/scheduled` is back, Scheduled is no longer in the removed list; every other e2e stays green.
  *Corrected 2026-09-15, before the page was built (CLAUDE.md §3.8):* `narrow-page-scheduled@390` shows the search field on the page at 390 (full width, the status filter beside it) and only the heading and Create moved into the bar — not the Assets page's bar Search. The page follows the capture: heading in the bar at 390, Create in the bar at both widths, the search field on the page at both.

## Departures from the reference

- The reference's Scheduled is the agent's timed tasks (run at a time / on a repeat); ours is a generation queue — the owner's meaning. The chrome is the reference's; the rows, the Waiting section and reorder are ours.
- Timed runs are ours in shape (a "run at" on a queued generation, once); the reference's repeats ("every day at…") are not offered.

## Technical Notes

- `lib/queue-store.ts` (`/data/queue.json`: `{ id, request, referenceFiles, position, createdAt, notBefore? }`), `lib/queue-runner.ts` (server-side: `submitDue(now)` — for each waiting entry in order whose `notBefore` is absent or past, `POST /jobs` to the adapter while it answers 202; stop at the first `busy`); called from `GET /api/history` (the sidebar's poll, every navigation) and from the jobs status route when a job turns terminal — no separate process, nothing to keep alive; a timed job therefore goes on the first poll after its time while the app is open (the page says so). `PATCH /api/queue/:id { notBefore | null }`.
- `POST /api/jobs`: on 503 `busy` from the adapter, write the queue entry (moving the uploaded images under `uploads/<queue id>/`, STORY_032's directory) and answer 202 with `{ id, status: "waiting", position }`; `HistoryEntry.status` gains `waiting`; `job-status.ts` and the task page's indicator learn it; the polling of a waiting entry asks `GET /api/history/:id` until it has a job id, then the job.
- Routes: `GET /api/queue` (waiting, in order), `GET /api/queue/:id` (the request for Edit, with the images' URLs), `PATCH /api/queue/:id { position }`, `DELETE /api/queue/:id`; `POST /api/jobs` with `replaces: <queue id>` (JSON and multipart) rewrites that entry instead of appending (409 `already_started` when it has been submitted); the composer's `initialRequest` prop and a `queueId` in its state for the editing banner; `components/pages/ScheduledPage.tsx` at `app/app/scheduled/page.tsx`; the sidebar row and `route-title` / `activeRow` entries return.
- The stub: a `busy` script (answers 503 busy to creates until `/__stub/free`) so the e2e can fill the queue and release it.

## Testing Plan

- **Unit** — `queue-store.test.ts` (order, reposition, remove, `notBefore`, the file), `queue-runner.test.ts` (submits in order, skips a timed entry until its time without blocking the rest, stops at busy, marks an unknown job failed), `job-status.test.ts` (the waiting state), `route-title.test.ts` (`/scheduled` back), the "Not before" formatting.
- **Integration** — `queue.test.ts`: a create that meets 503 busy becomes a waiting entry with its images kept; `GET /api/history` submits it when the stub frees; the routes' reorder / remove / `notBefore`; `GET /api/queue/:id`; a create with `replaces` rewrites the entry in place (position and run-at kept, images replaced or kept) and is a 409 once it has started; a remove of a submitted job is a 409.
- **Component** — `ScheduledPage.test.tsx` (the sections, search, the status filter, move / remove / Run at / Edit, Stop, empty); `Composer.test.tsx` (the editing banner from `initialRequest`, Send posts `replaces`, Cancel editing); `TaskPage.test.tsx` (the waiting indicator); `Sidebar.test.tsx` (the row).
- **E2E** — `scheduled.spec.ts` (new): the stub `busy`; send two prompts → both Waiting on the page in order → move the second up → put a "run at" an hour ahead on one → free the stub → the untimed one runs and finishes (terminal waited on), the timed one stays "Not before …" → clear its time → it goes and finishes; Edit a waiting one (the composer prefilled, a changed prompt, Send → the same row with the new title, no new job); Remove a waiting one; at 390 through the drawer. The STORY_026 sweep adjusted.

## Estimated Complexity

Large — a second kind of pending work, a runner, a page with Edit through the composer, and the stub's new script.

## Handover (2026-09-15 13:06 EDT — the session stopped here at the owner's request; the next session continues from this section)

**For the next session (the owner's instruction, 2026-09-15): work one story at a time.** This is the only approved story left — one story, three stages; finish stage two, then stage three, then land and deploy it, and only then read EPIC_007's candidate rows (042–044), which are not stories until the owner approves a draft. Do not open a second story in parallel.

**Where it stands.** Stage one of three — the server side — is written and green at the unit and integration layers (`tools/gate/run.sh typecheck lint test test:integration`); it is landed as a partial commit labelled "stage one" if the full gate passed at the end of the session (see the commit log), otherwise it is in the working tree uncommitted. Stages two (the UI) and three (the e2e and the manual verification) are not started. The estimate in EPIC_007 (≈ 15:20 EDT) no longer holds; the next session sets a new one.

**Stage one — done (files, what each does):**
- `tools/stub-generation-server/src/server.ts` — `POST /__stub/busy { busy }` and the in-process `setBusy(busy)`: while set, `POST /jobs` answers 503 `busy` as the adapter does at its open-job limit; `reset` clears it. Test in `server.test.ts` ("STORY_041: the busy hook"). `app/e2e/fixtures/stub.ts` gained `stubApi.busy(on)`.
- `app/lib/queue-store.ts` — `queue.json` beside the history file; `enqueue` (appends, returns the 1-based position), `waiting()` (in line order), `getQueued`, `replaceQueued` (Edit, in place), `moveQueued(id, "up" | "down")`, `setNotBefore`, `markSubmitted(id, jobId)`, `removeQueued`, `due(now)` (waiting, untimed or past their time — a timed one is skipped, never blocking). Test: `queue-store.test.ts`.
- `app/lib/queue-runner.ts` — `submitDue({ now?, submit? })`: for each due entry in order, POST to the model server (JSON, or multipart with the images read from `uploads/<id>/`, STORY_032's directory; only the contract's fields — never `projectId`, `notBefore`, `replaces`; the stub's `script` forwarded as on the original URL); 202 → `markSubmitted` + the history entry gets `jobId`; busy / unreachable / 502 / 503 → the line waits; any other refusal → the entry is failed in history with the server's message and leaves the line, the runner moves on. One run at a time (a second call joins the first). `upstreamJobId(id)` = the history entry's `jobId` or the id itself. Test: `queue-runner.test.ts` (an injected `submit`).
- `app/app/api/jobs/route.ts` — the create path: `notBefore` → always queued (`queued()`), `replaces: <queue id>` → `replaced()` (rewrite in place; new images replace, none sent keeps; 404 unknown, 409 `already_started`); otherwise forwarded, and a 503 `busy` from the server → queued. A queued request gets a fresh UUID as both its queue id and its history id (`historyStore().create`, status `queued`), answers 202 `{ id, status: "queued", progress: 0, position }`. `APP_ONLY` = the fields stripped from what goes upstream.
- `app/app/api/jobs/[id]/route.ts` — GET: runs `submitDue()` first; a waiting entry answers `{ id, status: "queued", progress: 0, position, notBefore? }`; a submitted one is asked of the server under `upstreamJobId(id)` and answered under ours (recorded into history under ours). DELETE: a waiting entry leaves the line and is recorded cancelled (202); otherwise forwarded under the upstream id. `result` and `poster` routes map the id the same way.
- `app/app/api/queue/route.ts` (GET the line: `{ entries: [{ id, position, title, createdAt, notBefore?, referenceImages, projectId }] }`, runs the runner first) and `app/app/api/queue/[id]/route.ts` (GET the request for Edit — `{ id, position, request, notBefore | null, images: [{ n, name, type, url }] }`; PATCH `{ move }` or `{ notBefore | null }`; DELETE — Remove: the entry, its history entry and its uploads go; 409 once submitted).
- `app/app/api/history/route.ts` and `…/[id]/route.ts` — the list runs `submitDue()` first (every sidebar poll advances the line); forgetting an entry also removes it from the queue.
- `app/lib/job-api.ts` (`JobStatusResponse.position?`), `app/lib/job-status.ts` (`JobSnapshot.position`, carried by a queued status, dropped when it runs), `app/lib/todo-steps.ts` (`ordinal`, "Waiting — 2nd in line"), `app/lib/history-store.ts` (`HistoryEntry.jobId`; `patch` retitles when the prompt changes; the patch keys for Edit).
- Integration: `app/test/integration/queue.test.ts` — busy → the line with positions → freed → submitted under the same id through to the result; a timed request waits while an untimed one goes, the images travel; move / Edit / Remove / cancel of a waiting one; Edit and Remove refused once submitted.

**Stage two — to do (the UI):**
1. `app/lib/route-title.ts`: `/scheduled` back in `REFERENCE_PAGES` as `"scheduled"` and `ActiveRow` gains `"scheduled"`; `route-title.test.ts` follows. `components/shell/Sidebar.tsx`: `{link("/scheduled", "scheduled", <IconClock />, "Scheduled")}` between Plugins and Assets (and the rail pill); `Sidebar.test.tsx`'s row lists gain "Scheduled"; `components/removed.test.tsx` drops "Scheduled" from `REMOVED_CONTROLS`; `e2e/shell.spec.ts` drops `/scheduled` from the 404 sweep.
2. `app/app/scheduled/page.tsx` → `components/pages/ScheduledPage.tsx` (the mockup above; chrome from `page-scheduled@1440`): fetch `GET /api/queue` and `GET /api/history` on mount and every 2 s while mounted (an interval, cleared on unmount — StrictMode-safe); sections **Running** (history entries with status `queued` / `running` that are not in the waiting list — they have a `jobId` or were created directly — with progress and a Stop that calls `DELETE /api/jobs/:id`), **Waiting** (the line: position, stamp + title, "Not before …" via a `formatNotBefore(iso, now)` in `lib/queue-view.ts`, ↑ ↓ → `PATCH /api/queue/:id { move }`, **Run at…** → an inline `<input type="datetime-local">` → `PATCH { notBefore }` / clear → `{ notBefore: null }`, **Edit** → `router.push("/?queue=" + id)`, **Remove** → `DELETE /api/queue/:id`, no confirm), **Done today** and **Failed** (history entries terminal in the last 24 h, each a link to `/task/:id`); the search field filters every section by title; the status filter (All · Waiting · Running · Done · Failed) is a select like Archived tasks' project filter; the empty state "No scheduled tasks yet." with a Create button (a link to `/`); the page puts a "+ Create" link in the Shell's top bar through `setPageActions` (the Assets page shows how). `ScheduledPage.test.tsx` per the Testing Plan.
3. `components/composer/Composer.tsx`: an `initialRequest` prop (`{ queueId, prompt, ratio, resolution, durationSeconds, model, projectId?, notBefore?, images: [{ n, name, url }] }`) — text and project from it, `enter-video-mode`, and once capabilities arrive dispatch `scene` with its prompt / ratio / resolution / duration and `model`; fetch each image URL into a `File` and `add-images`; an "Editing a queued job — Send replaces it" banner with a **Cancel editing** link to `/scheduled`; `composer-state.ts` gains `queueId?: string` and `notBefore?: string` (a `not-before` action); `submit-job.ts`'s `buildJobRequest` sends `replaces` and `notBefore` when set; after a 202 whose body has `position`, the composer toasts "Queued — Nth in line" (`useShell().notify`; `SubmitResult` gains `position?`) and, when editing, navigates to `/scheduled` instead of the task page. A **Run at…** control beside Send (home variant, video mode) sets `notBefore`. `Composer.test.tsx` per the Testing Plan.
4. `app/app/page.tsx`: `?queue=<id>` → `getQueued(id)` (server-side, `lib/queue-store`) → `initialRequest` (images via `referenceUrl`), keyed like `?project=` / `?skill=`.
5. README: the Features row for STORY_041 and the "What was removed" row (Scheduled is back with our meaning); the Running the Model measured table gains the queued pair once verified.

**Stage three — to do:** `e2e/scheduled.spec.ts` exactly as the Testing Plan reads (`stubApi.busy(true)` to fill the line, `busy(false)` to release; every job's terminal status waited on with `waitForTerminalStatus` — the queued job's id is the history id, which the status route answers under); the manual verification on the Spark (two real prompts queued with ComfyUI up; the second waits for the first; both finish; the date, the model and the times into the Done note and the README's measured table); the Done note; EPIC_007's Actual done.

**Decisions already taken (keep them unless the owner says otherwise):** a queued request keeps one id everywhere (the queue's = the history's; the model server's lives in `jobId`); a request with `notBefore` is always queued, never forwarded, even when the server is free; `replaces` never forwards; the runner lives in the polled routes (no process of its own — STORY 042 in EPIC_007 is where that would change); Remove deletes the history entry and its uploads (it was never a job); cancelling a waiting request from its task page records it cancelled and leaves the line; a refusal that is not busy fails the entry and the line moves on.

## Done (2026-09-15 — two sessions; stage one landed as `c17cb0d`, stages two and three as `4d7f229`)

**Landed.** *The server side (stage one, the Handover above lists every file):* `lib/queue-store.ts` (`/data/queue.json`), `lib/queue-runner.ts` (`submitDue` inside the polled routes — the history list, a job's status, the queue list — no process of its own), `POST /api/jobs` queuing on the adapter's 503 `busy` or on a `notBefore`, rewriting in place on `replaces`; `GET/PATCH/DELETE /api/queue[/:id]`; the status, cancel, result and poster routes answering under the history id and asking the adapter under the job it became (`HistoryEntry.jobId`); the stub's `POST /__stub/busy`. *The UI (stage two):* the Scheduled row and rail pill are back (`/scheduled` in `REFERENCE_PAGES`); `components/pages/ScheduledPage.tsx` — the capture's chrome (the heading on the page at 1440 and in the bar at 390, the search field with the status filter, Create in the bar and in the empty state) and our sections: **Running** (the adapter's open jobs, polled by the page for their progress, with Stop), **Waiting** (the line in order: position, stamp and title, "Waiting · next" / "Waiting" / "Not before …", ↑ ↓, **Run at…** — an inline date-time field with a Clear and the note that the queue runs inside the app —, **Edit** → `/?queue=<id>`, **Remove**, no confirm), **Done today** and **Failed** (the last 24 h, each a link to its task); the page refetches every two seconds while open (a StrictMode-safe interval) and so advances the line. The composer: `initialRequest` (the request as sent, from `getQueued` on the home route) starts it in video mode with the prompt, project and run-at, applies the parameters and refetches the images once the Spark's capabilities are known, shows "Editing a queued job — Send replaces it, Cancel editing keeps it" with a Cancel editing link; **Run at…** beside Send (video mode, home) sets `notBefore`; Send posts `replaces` / `notBefore`, toasts "Queued — Nth in line" when the answer carries a position, and returns to Scheduled after an Edit; the task page's indicator reads "Waiting — Nth in line" (`ordinal`). `lib/queue-view.ts` holds the sections, the search and filter, and the "Not before" formatting. *Stage three:* `e2e/scheduled.spec.ts`.

**Tests.** Unit: `queue-store.test` (order, move, time, submit, replace, remove, garbage), `queue-runner.test` (in order; a timed entry skipped, never blocking; busy → the line waits; a refusal fails the entry and the runner moves on; unreachable waits; one run at a time; only the contract's fields go up), `queue-view.test` (the sections, search, filter, the time formats, the running label), `job-status.test` and `todo-steps.test` (the position and the ordinal indicator), `composer-state.test` (`not-before`, `initialComposer`'s extras), `submit-job.test` (`replaces` / `notBefore` in JSON and multipart; the position back), `route-title.test` (`/scheduled` back). Integration: `queue.test` (busy → the line with positions → freed → submitted under the same id through to the result; a timed request waits while an untimed one goes, its image travels; move / Edit / Remove / cancel of a waiting one; Edit and Remove refused once submitted), the stub's `server.test` (the busy hook). Component: `ScheduledPage.test` (empty state and the bar's Create; the four sections with their words; Stop / move / Remove / Edit call their routes; search and filter; Run at… patches a time and Clear sends null; the 2 s poll under StrictMode, one tick per interval, none after unmount), `Composer.test` (Edit: video mode, the words, parameters and image once capabilities arrive, the banner and its link, Send posts `replaces` and `notBefore` as multipart and returns to Scheduled with the toast; Run at… sets, × clears, a queued Send toasts its place), `TaskPage.test` (the waiting indicator, then the job as usual), `Sidebar.test` (the row and the rail pill). E2E: `scheduled.spec.ts` at desktop and narrow — the stub busy; two prompts queued with their toasts and the task page's "Waiting — 1st in line"; the Scheduled row through the drawer at 390; the line in order; the second moved up; a run-at an hour ahead ("Not before …"); the stub freed: the untimed one runs (seen Running) and finishes (terminal waited on) while the timed one waits; the time cleared: it goes and finishes; its result served under our id; a third queued, edited through the composer (prefilled, the banner, a new prompt, Send → 202, back on Scheduled with the new title, no new history entry) and removed (204, gone from history, the server never asked); the empty page, search and filter. The STORY_025/026/028 sweep now expects `/scheduled` 200. Gate by hand: typecheck, lint, unit (226), integration (29), build + image, e2e 101 passed / 9 skipped; and again in the pre-push hook for both commits.

**Manual verification (the Spark, 2026-09-15, MiniMax-H3 FL2VA through the adapter 1.4.0, ComfyUI up, the peer session told before and after; the app container on `4d7f229`).** Two 5 s text-to-video prompts through `POST /api/jobs`: **A** at 13:52:43 EDT went straight to the adapter (openJobs 1) and finished at **14:10:00** — 17 min 17 s, 5.167 s, 621,118 B, `cuts: []`; **B**, sent the same second with a run-at of 14:11:43, sat in the app's queue at position 1 ("Not before …" on the page — see the two screenshots taken from the live container at 1440 and 390 while A ran: Running "Generating 9 %" with Stop, Waiting "1 · Not before …" with ↑ ↓ Run at… Edit Remove, the day's earlier jobs under Done today; at 390 the heading in the bar beside Create), was submitted by the runner on the first poll after its time (**14:12:00**, 17 s later) under the adapter's job `0cd46895…` while keeping our id, and finished at **14:29:33** — 17 min 33 s, 5.167 s, 358,719 B, `cuts: []`; its result is served under our id. The two were never on the GPU together (A done at 14:10:00, B started 14:12:00); afterwards the queue is empty and the adapter reports openJobs 0. Memory was not sampled this run (the README's 64 GiB for a 5 s job stands from STORY_005/006).

**Side by side.** `page-scheduled@1440` / `narrow-page-scheduled@390`: the heading, the search field and its "All ▾" filter, Create top-right (black, with a +) and "No scheduled tasks yet." are the capture's; ours has no "More create actions ▾" split on Create (nothing to split) and adds the sections below — the rows are ours (Departures). The 390 layout follows the capture (heading in the bar; the search field on the page), not the AC's first phrasing — corrected above.

**Departures added while building:** the page's Running rows poll each job's status themselves (history learns progress only from a status poll); a request with a run-at is always queued even when the adapter is free (so the time is honoured); Remove deletes the history entry and its images (it was never a job); the stub's busy hook is `POST /__stub/busy { busy }` rather than the Technical Notes' `/__stub/free`.
